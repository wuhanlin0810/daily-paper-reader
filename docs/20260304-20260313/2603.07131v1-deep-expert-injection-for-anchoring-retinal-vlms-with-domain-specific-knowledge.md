---
title: Deep Expert Injection for Anchoring Retinal VLMs with Domain-Specific Knowledge
title_zh: 深度专家注入：利用领域特定知识锚定视网膜视觉语言模型
authors: "Shuai Lu, Meng Wang, Jia Guo, Jiawei Du, Bo Liu, Shengzhu Yang, Weihang Zhang, Huazhu Fu, Huiqi Li"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07131v1"
tags: ["query:med-mm"]
score: 10.0
evidence: 用于诊断的视网膜视觉语言模型 (VLM)
tldr: 针对大型视觉语言模型在眼科诊断中存在的感知缺失和推理偏差问题，本文提出EyExIn框架。通过深度专家注入机制，该框架利用双流编码解耦解剖背景与病理语义，并引入自适应门控融合与深度专家注入技术，将视觉锚点嵌入大语言模型中间层，有效抑制幻觉并增强领域知识，在多个眼科问答基准上达到SOTA水平。
selection_source: fresh_fetch
motivation: 通用视觉编码器难以识别细微病变，且深层语言先验易覆盖视觉证据导致模型产生幻觉。
method: 提出EyExIn框架，采用双流编码策略、语义自适应门控融合以及将视觉特征作为残差偏置注入LLM中间层的深度专家注入机制。
result: 在四个眼科基准测试中，该模型性能一致优于大型商业系统，实现了最先进的眼科视觉问答精度。
conclusion: 该研究通过深度专家注入有效增强了眼科领域知识的嵌入，为开发可靠的眼科人工智能提供了新思路。
---

## 摘要
大语言视觉模型 (LVLMs) 在自动化眼科诊断方面展现出巨大潜力。然而，由于缺乏领域特定知识，它们的临床部署受到严重阻碍。在这项工作中，我们识别出阻碍可靠医学推理的两个结构性缺陷：1) 感知鸿沟 (Perception Gap)，即通用视觉编码器无法解析细粒度的病理线索（如微动脉瘤）；2) 推理鸿沟 (Reasoning Gap)，即在更深的 Transformer 层中，稀疏的视觉证据逐渐被海量的语言先验所覆盖，导致产生无根据的幻觉。为了弥合这些差距，我们提出了 EyExIn，这是一个高效利用数据的框架，旨在通过深度专家注入机制，利用专家知识锚定视网膜 VLMs。我们的架构采用了专家感知的双流编码策略，将视觉表示解耦为用于解剖背景的通用流和用于病理语义的专门专家流。为了确保高保真集成，我们设计了一个语义自适应门控融合模块，该模块在过滤无关背景噪声的同时，动态放大细微的病变信号。此外，我们引入了自适应深度专家注入，通过将融合后的视觉特征作为残差偏置直接集成到大语言模型 (LLM) 中间层，从而嵌入持久的“视觉锚点”。该机制创建了一个视觉快捷路径，迫使推理堆栈严格基于视觉证据。在四个基准测试上的广泛实验表明，我们的模型始终优于大型专有系统。EyExIn 显著增强了领域特定知识的嵌入，并在眼科视觉问答中实现了最先进的精度，推动了可靠眼科人工智能的发展。

## Abstract
Large Vision Language Models (LVLMs) show immense potential for automated ophthalmic diagnosis. However, their clinical deployment is severely hindered by lacking domain-specific knowledge. In this work, we identify two structural deficiencies hindering reliable medical reasoning: 1) the Perception Gap, where general-purpose visual encoders fail to resolve fine-grained pathological cues (e.g., microaneurysms); and 2) the Reasoning Gap, where sparse visual evidence is progressively overridden by massive language priors in deeper transformer layers, leading to ungrounded hallucinations. To bridge these gaps, we propose EyExIn, a data-efficient framework designed to anchor retinal VLMs with expert knowledge via a Deep Expert Injection mechanism. Our architecture employs an Expert-Aware Dual-Stream encoding strategy that decouples visual representation into a general stream for anatomical context and a specialized expert stream for pathological semantics. To ensure high-fidelity integration, we design a Semantic-Adaptive Gated Fusion module, which dynamically amplifies subtle lesion signals while filtering irrelevant background noise. Furthermore, we introduce Adaptive Deep Expert Injection to embed persistent "Vision Anchors" by integrating fused visual features as residual biases directly into intermediate LLM layers. This mechanism creates a visual shortcut that forces the reasoning stack to remain strictly grounded in visual evidence. Extensive experiments across four benchmarks demonstrate that our model consistently outperforms massive proprietary systems. EyExIn significantly enhances domain-specific knowledge embedding and achieves state-of-the-art precision in ophthalmic visual question answering, advancing the development of trustworthy ophthalmic AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《Deep Expert Injection for Anchoring Retinal VLMs with Domain-Specific Knowledge》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（LVLMs）在眼科自动诊断中面临两个结构性缺陷：
    1.  **感知鸿沟（Perception Gap）**：通用视觉编码器（如 CLIP）难以识别细微的病理特征（如微动脉瘤）。
    2.  **推理鸿沟（Reasoning Gap）**：在深层 Transformer 中，稀疏的视觉信号容易被强大的语言先验覆盖，导致模型产生脱离图像事实的“幻觉”诊断。
*   **研究背景**：眼科诊断高度依赖精细视觉证据，且专家标注数据稀缺。现有的“暴力”扩展数据规模的方法在医疗领域成本极高且难以实现。

### 2. 方法论：EyExIn 框架
该论文提出了 **EyExIn** 框架，核心思想是通过“深度专家注入”将领域知识锚定在模型中：
*   **专家感知双流编码（Expert-Aware Dual-Stream Encoding）**：
    *   **通用流**：保留冻结的通用视觉编码器，捕捉宏观解剖结构和色彩变化。
    *   **专家流**：引入对比预训练的视网膜基础模型，专门提取细粒度病理语义。
*   **语义自适应门控融合（Semantic-Adaptive Gated Fusion, GF）**：
    *   利用轻量级语义路由计算 Token 级别的权重图。通过凸插值动态放大病变区域信号，同时过滤背景噪声，提高视觉信噪比。
*   **自适应深度专家注入（Adaptive Deep Expert Injection）**：
    *   **视觉锚点（Vision Anchors）**：不再仅在输入端融合，而是将融合后的视觉特征作为残差偏置直接注入 LLM 的中间层。
    *   **双门控设计**：Token 级别路由检测表示衰减并刷新病理特征；层级别缩放参数（零初始化）确保训练初期不破坏 LLM 的预训练状态。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：15万张真实临床眼底图像及对应诊断报告。
    *   **测试基准**：涵盖 4 个数据集：TM4K（私有临床数据，21类）、JSIEC（39类）、Retina（3类）、ODIR（7类）。
*   **任务类型**：闭口 VQA（分类/诊断）和开口 VQA（描述/推理）。
*   **对比方法**：
    *   **商业模型**：Qwen3-VL-Max, ChatGPT-5.2, Gemini3-Pro。
    *   **开源微调模型**：LLaVA, Qwen2.5-VL (FT)。
*   **评价指标**：Macro-F1、Recall、Precision（针对诊断）；BLEU-1、ROUGE-L、BERT-F1（针对文本生成）。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA A100 GPU 进行训练。
*   **软件/框架**：采用 DeepSpeed ZeRO-2 优化器。
*   **微调技术**：使用 LoRA（Rank=16, Alpha=32）对 7B 规模的 LLM 主干进行参数高效微调。
*   **训练细节**：学习率 $2 \times 10^{-5}$，Batch Size 为 48，最大序列长度 2048，采用 BFloat16 混合精度。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同分布、不同设备采集的数据集上进行了验证，确保了泛化性。
*   **消融实验**：专门针对“专家流”、“门控融合”和“深度注入策略”进行了对比实验（见表3），验证了各组件对 Precision 和 Recall 的具体贡献。
*   **客观性**：对比了当前最顶尖的商业模型（如 GPT-5.2 级别），且使用了 Macro-Average 指标，充分考虑了医疗数据中长尾/罕见疾病的公平性。

### 6. 主要结论与发现
*   **性能领先**：EyExIn (7B) 在多个基准上显著优于参数量更大的商业模型（如在 TM4K 上 F1 达到 78.07%，远超 Gemini3-Pro 的 37.99%）。
*   **抑制幻觉**：通过深度注入视觉锚点，模型在开口问答中的精确度大幅提升，有效解决了语言先验主导导致的误诊问题。
*   **数据效率**：证明了在有限的医疗数据下，结构化的专家知识注入比单纯的指令微调更有效。

### 7. 优点与亮点
*   **结构创新**：不同于常见的“早融合”架构，提出的“深度注入”为解决 VLM 视觉信号衰减提供了新思路。
*   **临床实用性**：模型能够输出准确的定量指标（如杯盘比 C/D ratio），更符合临床医生的需求。
*   **即插即用**：该机制可以推广到其他对细粒度视觉要求极高的医学影像领域。

### 8. 不足与局限
*   **数据集依赖**：虽然在公共集上表现良好，但核心训练依赖于 15 万张高质量私有临床数据，其他研究者难以完全复现训练过程。
*   **模态局限**：目前仅聚焦于眼底彩色照相（Fundus），未涉及 OCT、超声等其他眼科多模态影像。
*   **计算开销**：虽然使用了 LoRA，但双流编码和多层特征注入相比标准 VLM 增加了推理时的计算负担。

（完）
