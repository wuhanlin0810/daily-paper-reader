---
title: Federated Modality-specific Encoders and Partially Personalized Fusion Decoder for Multimodal Brain Tumor Segmentation
title_zh: 用于多模态脑肿瘤分割的联邦模态特定编码器与部分个性化融合解码器
authors: "Hong Liu, Dong Wei, Qian Dai, Xian Wu, Yefeng Zheng, Liansheng Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04887v1"
tags: ["query:med-mm"]
score: 10.0
evidence: 具有模态特定编码器和融合解码器的多模态脑肿瘤分割
tldr: 针对联邦学习中多模态医学图像分析面临的模态缺失（模态间异构性）和个性化需求，本文提出FedMEPD框架。该框架采用联邦模态特定编码器处理异构数据，并通过动态更新差异实现解码器的部分个性化。此外，利用服务器端的全模态锚点和交叉注意力机制，帮助缺失模态的客户端补偿信息损失。在BraTS数据集上的实验证明，该方法在多模态脑肿瘤分割任务中优于现有先进方法。
selection_source: fresh_fetch
motivation: 现有的联邦学习方法难以处理客户端间模态不完整导致的异构性，且缺乏针对本地数据特征的个性化模型支持。
method: 提出FedMEPD框架，结合联邦模态特定编码器、部分个性化融合解码器，并利用全局全模态锚点引导缺失模态的表示校准。
result: 在BraTS 2018和2020基准测试中，该方法在多模态和个性化联邦学习任务上均取得了超越现有主流技术的效果。
conclusion: FedMEPD通过模态特定建模和动态个性化策略，有效解决了联邦学习中多模态数据缺失与异构性带来的挑战。
---

## 摘要
现有的医学图像分析联邦学习（FL）方法大多仅考虑了模态内异质性，限制了其在多模态成像应用中的适用性。在实践中，部分联邦学习参与者可能仅拥有完整成像模态的一个子集，这带来了模态间异质性挑战，难以在所有参与者的数据上有效训练全局模型。同时，每个参与者都希望在联邦学习中获得针对其本地数据特征定制的个性化模型。本研究提出了一种新的联邦学习框架，即具有联邦模态特定编码器和部分个性化多模态融合解码器的 FedMEPD，以同时解决这两个问题。具体而言，FedMEPD 为每个模态采用专属编码器以应对模态间异质性。虽然这些编码器是完全联邦化的，但解码器是部分个性化的，以满足个体需求——利用全局和本地参数更新之间的差异来动态确定哪些解码器滤波器是个性化的。在实现方面，拥有全模态数据的服务器采用融合解码器来融合来自所有模态特定编码器的表示，从而桥接各模态，通过反向传播优化编码器。此外，从融合的多模态表示中提取多个锚点（anchors），并与模型参数一起分发给客户端。相反，模态不完整的客户端通过缩放点积交叉注意力机制，将其缺失模态的表示向全局全模态锚点进行校准，从而弥补因模态缺失造成的信息损失。FedMEPD 在 BraTS 2018 和 2020 多模态脑肿瘤分割基准数据集上进行了验证。结果表明，该方法优于多种最新的多模态和个性化联邦学习方法，且其新颖设计是有效的。

## Abstract
Most existing federated learning (FL) methods for medical image analysis only considered intramodal heterogeneity, limiting their applicability to multimodal imaging applications. In practice, some FL participants may possess only a subset of the complete imaging modalities, posing intermodal heterogeneity as a challenge to effectively training a global model on all participants' data. Meanwhile, each participant expects a personalized model tailored to its local data characteristics in FL. This work proposes a new FL framework with federated modality-specific encoders and partially personalized multimodal fusion decoders (FedMEPD) to address the two concurrent issues. Specifically, FedMEPD employs an exclusive encoder for each modality to account for the intermodal heterogeneity. While these encoders are fully federated, the decoders are partially personalized to meet individual needs -- using the discrepancy between global and local parameter updates to dynamically determine which decoder filters are personalized. Implementation-wise, a server with full-modal data employs a fusion decoder to fuse representations from all modality-specific encoders, thus bridging the modalities to optimize the encoders via backpropagation. Moreover, multiple anchors are extracted from the fused multimodal representations and distributed to the clients in addition to the model parameters. Conversely, the clients with incomplete modalities calibrate their missing-modal representations toward the global full-modal anchors via scaled dot-product cross-attention, making up for the information loss due to absent modalities. FedMEPD is validated on the BraTS 2018 and 2020 multimodal brain tumor segmentation benchmarks. Results show that it outperforms various up-to-date methods for multimodal and personalized FL, and its novel designs are effective.

---

## 论文详细总结（自动生成）

这是一份关于论文《Federated Modality-specific Encoders and Partially Personalized Fusion Decoder for Multimodal Brain Tumor Segmentation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在医学影像分析中，联邦学习（FL）常用于保护隐私。然而，现有的 FL 方法多关注模态内异质性（如不同设备的成像差异），忽略了**模态间异质性**（Intermodal Heterogeneity）。
*   **核心问题**：在实际场景中，不同医疗机构（客户端）拥有的 MRI 序列（模态）往往不完整（如缺失 T1c 或 FLAIR）。这导致难以训练一个统一的全局模型，且各机构对针对其特定模态组合的“个性化模型”有强烈需求。
*   **研究动机**：旨在开发一个既能利用服务器全模态数据提升全局性能，又能为缺失模态的客户端提供高性能个性化分割模型的联邦学习框架。

### 2. 论文提出的方法论：FedMEPD
该框架的核心思想是“编码器共享、解码器部分个性化、特征对齐校准”。
*   **联邦模态特定编码器**：为 T1、T1c、T2、FLAIR 四种模态分别设置独立的编码器。这些编码器在拥有相应模态的客户端与服务器之间完全共享（联邦化），以学习特定模态的通用特征。
*   **部分个性化融合解码器**：
    *   **动态判定**：利用全局更新（服务器）与本地更新（客户端）之间参数的**余弦相似度**来衡量一致性。
    *   **滤波器级个性化**：如果某个滤波器的更新方向在多个回合内持续不一致，则将其设为“本地个性化”，不再参与联邦聚合；反之则继续联邦化。这平衡了通用知识共享与本地特性保留。
*   **多锚点多模态表示与 LACCA 模块**：
    *   **服务器端**：从全模态融合特征中通过 K-means 聚类提取多个“锚点”（Anchors），代表不同类别的典型多模态特征。
    *   **客户端校准（LACCA）**：客户端接收这些锚点，通过**缩放点积交叉注意力机制**（Cross-Attention），将本地缺失模态的特征向全局全模态锚点进行校准，从而在特征层面“补偿”缺失的信息。

### 3. 实验设计
*   **数据集**：
    *   **BraTS 2018 & 2020**：脑肿瘤分割基准，包含四种 MRI 模态。
    *   **HaN-Seg**：头颈部器官分割（CT 与 MRI 模态），用于验证泛化性。
*   **场景设置**：模拟了 8 个客户端，分别持有单模态、双模态、三模态和全模态数据，服务器持有全模态数据。
*   **对比方法（Benchmark）**：
    *   **基础 FL**：FedAvg。
    *   **个性化 FL**：perFL, FedNorm, IOP-FL, FedCostWAvg, FedPIDAvg。
    *   **多模态 FL**：FedMSplit, FedIoT, CreamFL。
    *   **集中式基准**：本地训练模型（Local models）和 RFNet（针对缺失模态的 SOTA 模型）。

### 4. 资源与算力
*   **硬件环境**：使用了 **5 台 NVIDIA RTX 2080Ti GPU**。
*   **分配方式**：服务器占用 1 台，其余 4 台平均分配给各个客户端。
*   **软件框架**：PyTorch 1.13.0。
*   **训练细节**：总计训练 1000 个联邦回合（Rounds），每个回合本地训练 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 BraTS 数据集上进行了完整的性能对比。
    *   **消融实验**：详细验证了锚点数量（$N_k$）、特征层级（$l$）、个性化耐心值（$P$）的影响。
    *   **鲁棒性实验**：测试了服务器数据量减少（10%~50%）以及标注质量下降（人为腐蚀/膨胀）对结果的影响。
    *   **泛化实验**：在 HaN-Seg 数据集上验证了跨任务的适用性。
*   **评价**：实验设计非常充分，考虑了数据量、标注噪声、客户端数量及异构程度等多种变量，对比方法涵盖了近年来的主流技术，实验过程具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：FedMEPD 在服务器端（全模态）和客户端（平均性能）上均显著优于现有方法。在 BraTS 2018 上，客户端平均 Dice 分数比次优方法高出约 5%。
*   **双向提升**：该框架不仅提升了缺失模态客户端的分割精度，还通过有效利用客户端的异构数据，反哺并增强了服务器的全模态全局模型。
*   **校准有效性**：通过交叉注意力机制引入全局锚点，能有效弥补单模态或少模态客户端的信息缺失。

### 7. 优点（亮点）
*   **动态个性化策略**：不采用固定的层级拆分（如只个性化最后几层），而是根据参数更新的一致性动态决定滤波器的个性化，更加精细。
*   **多锚点机制**：相比于单一原型（Prototype），多锚点能更好地捕捉 3D 医学影像中复杂的解剖变异。
*   **隐私友好**：传输的是高度抽象的类锚点而非原始特征图，降低了隐私泄露风险。

### 8. 不足与局限
*   **服务器依赖性**：该框架假设服务器拥有高质量的全模态数据。虽然实验证明了其对服务器数据量减少具有一定鲁棒性，但在完全没有全模态参考源的极端去中心化场景下，该方法可能失效。
*   **统计显著性挑战**：由于 BraTS 数据集按机构划分后，单个客户端的测试样本量较小（20-35例），部分性能提升在统计学上的显著性（p-value）在某些指标上不够突出。
*   **计算开销**：虽然推理速度较快，但引入交叉注意力机制和多模态编码器会增加本地训练的显存占用和计算复杂度。

（完）
