---
title: Adaptive Clinical-Aware Latent Diffusion for Multimodal Brain Image Generation and Missing Modality Imputation
title_zh: 用于多模态脑图像生成和缺失模态插补的自适应临床感知潜在扩散模型
authors: "Rong Zhou, Houliang Zhou, Yao Su, Brian Y. Chen, Yu Zhang, Lifang He, Alzheimer's Disease Neuroimaging Initiative"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09931v1"
tags: ["query:med-mm"]
score: 10.0
evidence: 用于多模态脑图像生成和缺失模态插补的自适应融合
tldr: 针对阿尔茨海默病诊断中多模态神经影像数据缺失的问题，本文提出ACADiff框架。该框架利用自适应临床感知潜扩散模型，结合GPT-4o编码的临床元数据，在sMRI、FDG-PET和AV45-PET之间进行双向合成。实验证明，ACADiff在极高缺失率下仍能保持优异的生成质量和诊断性能，显著优于现有基准。
selection_source: fresh_fetch
motivation: 临床数据集中多模态神经影像的缺失严重阻碍了阿尔茨海默病的准确诊断和多模态信息的互补利用。
method: 提出一种基于潜扩散的ACADiff框架，通过自适应融合机制和GPT-4o生成的临床语义引导来合成缺失模态。
result: "在ADNI数据集上，该方法在80%数据缺失的极端情况下依然实现了领先的生成质量和诊断鲁棒性。"
conclusion: ACADiff为多模态脑影像缺失补全提供了高效方案，显著提升了临床诊断的可靠性与实用性。
---

## 摘要
多模态神经影像为阿尔茨海默病的诊断提供了互补的见解，但临床数据集经常面临模态缺失的问题。我们提出了 ACADiff，这是一个通过自适应临床感知扩散来合成缺失脑成像模态的框架。ACADiff 通过在关注可用成像数据和临床元数据的同时，逐步对潜在表示进行去噪，从而学习不完整多模态观测值与目标模态之间的映射。该框架采用了根据输入可用性动态重构的自适应融合机制，并结合了通过 GPT-4o 编码提示提供的语义临床指导。三个专门的生成器实现了 sMRI、FDG-PET 和 AV45-PET 之间的双向合成。在 ADNI 受试者上的评估结果表明，ACADiff 实现了卓越的生成质量，即使在 80% 缺失的极端情况下也能保持稳健的诊断性能，优于所有现有基准模型。为了提高可复现性，代码可在 https://github.com/rongzhou7/ACADiff 获取。

## Abstract
Multimodal neuroimaging provides complementary insights for Alzheimer's disease diagnosis, yet clinical datasets frequently suffer from missing modalities. We propose ACADiff, a framework that synthesizes missing brain imaging modalities through adaptive clinical-aware diffusion. ACADiff learns mappings between incomplete multimodal observations and target modalities by progressively denoising latent representations while attending to available imaging data and clinical metadata. The framework employs adaptive fusion that dynamically reconfigures based on input availability, coupled with semantic clinical guidance via GPT-4o-encoded prompts. Three specialized generators enable bidirectional synthesis among sMRI, FDG-PET, and AV45-PET. Evaluated on ADNI subjects, ACADiff achieves superior generation quality and maintains robust diagnostic performance even under extreme 80\% missing scenarios, outperforming all existing baselines. To promote reproducibility, code is available at https://github.com/rongzhou7/ACADiff

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Clinical-Aware Latent Diffusion for Multimodal Brain Image Generation and Missing Modality Imputation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在阿尔茨海默病（AD）的临床研究中，多模态神经影像（如 sMRI、FDG-PET、AV45-PET）对于全面评估病情至关重要，但现实中由于成本、采集协议差异或患者流失，数据集常面临严重的**模态缺失**问题。
*   **研究动机**：现有的生成模型（如 Pix2Pix、DS-GAN）存在训练不稳定或模式崩溃问题；而早期的扩散模型在处理不同输入组合时缺乏自适应融合机制，且未能充分利用临床元数据（如认知评分）的语义信息来引导图像生成。

### 2. 方法论
ACADiff 框架基于**潜在扩散模型（Latent Diffusion）**，核心思想是在压缩的潜在空间内，结合临床语义引导和自适应模态融合来合成缺失影像。
*   **潜在空间构建**：使用预训练的 3D VAE 将高维脑图像压缩为紧凑的潜在表示，降低计算开销。
*   **分层自适应条件（Hierarchical Adaptive Conditioning）**：
    *   **自适应图像调节**：根据输入模态的数量动态切换。若有两个输入（2→1），使用**交叉注意力（Cross-Attention）**进行模态间信息交换；若只有一个输入（1→1），则使用**可学习的 3D 卷积投影**。
    *   **语义临床引导**：利用 **GPT-4o** 将疾病诊断标签和连续认知评分（MMSE, ADAS13, CDR-SOB）编码为结构化文本提示（Prompt），通过交叉注意力机制引导去噪过程，确保生成的图像符合病理特征。
    *   **时间步调制**：利用 FiLM 机制根据扩散时间步 $t$ 动态调整去噪强度。
*   **训练目标**：结合了噪声预测损失（$L_{diff}$）和一致性正则化损失（$L_{cons}$），以保证生成模态与真实分布的一致性。

### 3. 实验设计
*   **数据集**：使用 ADNI 队列的 1,028 名受试者数据（包含 AD、MCI、HC 三类），涵盖 sMRI、FDG-PET 和 AV45-PET 三种模态。
*   **实验场景**：
    *   **图像生成评估**：测试 1→1 和 2→1 的双向合成质量。
    *   **下游诊断评估**：模拟 20%、40%、60%、80% 的数据缺失率，使用生成图像补全后进行 AD vs. HC 分类。
*   **对比基准（Benchmarks）**：
    *   GAN 类：Pix2Pix, DS-GAN（疾病感知 GAN）。
    *   扩散模型类：LDM（标准潜在扩散）、PASTA（病理感知）、FICD（功能约束）。
    *   消融对比：ACADiff-emb（使用简单嵌入而非 GPT-4o 编码）。

### 4. 资源与算力
*   **硬件设施**：实验在 **4 台 NVIDIA A100 GPU** 上运行。
*   **实现细节**：使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，扩散步数 $T=1000$。文本编码器采用 GPT-4o 的预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：使用了 1,028 例样本，在生成任务和分类任务上分别进行了独立划分（600 例用于生成器训练，428 例用于分类实验），有效避免了数据泄露。
*   **充分性**：
    *   涵盖了从轻微（20%）到极端（80%）的缺失率测试。
    *   进行了 10 次蒙特卡洛采样以保证推理结果的稳定性。
    *   通过与 ACADiff-emb 的对比，专门验证了 LLM 语义引导的有效性。实验设计客观、对比方法全面，具有较高的说服力。

### 6. 主要结论与发现
*   **生成质量领先**：ACADiff 在 PSNR、SSIM、NMI 等指标上均优于所有基准模型，PSNR 达到 27.9。
*   **诊断鲁棒性强**：在 80% 缺失的极端情况下，ACADiff 补全后的分类准确率仍能维持在 **77.5%**，而其他方法在此时性能大幅下降。
*   **语义引导价值**：使用 GPT-4o 编码的临床提示比传统的 Embedding 方式在 PSNR 上提升了约 1.8，证明了临床语义对病理模式生成的关键作用。

### 7. 优点
*   **灵活性**：单一模型即可处理多种输入组合（1→1 或 2→1），无需为不同缺失情况训练多个模型。
*   **多模态融合创新**：结合了 LLM 的语义理解能力与扩散模型的生成能力，开辟了“临床数据驱动影像合成”的新路径。
*   **临床实用性**：在极高缺失率下的稳定表现，使其非常适合实际临床中数据不全的场景。

### 8. 不足与局限
*   **计算依赖**：虽然在潜在空间操作，但 3D 扩散模型和 LLM 编码器的引入仍对算力有一定要求。
*   **黑盒风险**：依赖 GPT-4o 等外部大模型进行编码，其内部语义映射逻辑对医学专家而言可能缺乏一定的透明度。
*   **模态限制**：目前仅验证了三种核心模态，对于更多样化的影像（如 DTI、tau-PET）或其他生物标志物的扩展性有待进一步验证。

（完）
